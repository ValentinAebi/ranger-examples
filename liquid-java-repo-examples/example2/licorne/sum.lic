
typealias Nat = [0..]

object Test {

    fn sum(n: Int) -> Nat & [n..] {
        if (n <= 1) {
            return 0;
        } else {
            val t1 = sum(n-1);
            return n + t1;
        }
    }

}
