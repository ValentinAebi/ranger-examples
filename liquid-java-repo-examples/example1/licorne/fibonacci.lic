
typealias Nat = [0..]

object Fibonacci {

    fn fib(n: Nat) -> [0..] & [n..] {
        if (n <= 1) {
            return 0;
        } else {
            return fib(n-1) + fib(n-2);
        }
    }

}
